sudo: requried
services:
  - docker

before_install:
  - docker build -t monnish/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true monnish/docker-react npm run test -- --coverage